---
- name: daemon-reload
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart portal-server-agent
  ansible.builtin.systemd:
    name: portal-server-agent
    state: restarted
    daemon_reload: true
