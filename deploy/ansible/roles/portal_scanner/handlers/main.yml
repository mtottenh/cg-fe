---
- name: daemon-reload
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart portal-scanner
  ansible.builtin.systemd:
    name: portal-scanner
    state: restarted
    daemon_reload: true
