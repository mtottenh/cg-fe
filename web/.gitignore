# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env overrides — only .env.example and .env.production are tracked.
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# E2E test artifacts. The `-*` / `.*.` variants are the per-instance outputs
# from `scripts/e2e-ephemeral.sh -i N` (parallel agents).
e2e/.seeded-state.json
e2e/.seeded-state.*.json
playwright-report/
playwright-report-*/
test-results/
test-results-*/
scripts/ux/shots/
